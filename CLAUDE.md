# CLAUDE.md

このファイルは、[Claude Code](https://claude.com/claude-code) をはじめとする AI アシスタントが本リポジトリで作業する際のガイドラインです。

> **注意:** 現時点でリポジトリにはまだソースコードがコミットされていません。本ファイルは出勤管理システム（attendance-system）向けの**スターターテンプレート**です。実装が追加されたら、`[ ]` や `TODO` の箇所を実際の構成・コマンド・規約に合わせて更新してください。

---

## 1. プロジェクト概要

- **名称:** attendance-system（出勤管理システム）
- **目的:** 従業員・メンバーの出退勤の打刻、勤怠記録の集計、レポート出力などを行うシステム。
- **想定する主な機能（例）:**
  - 出勤・退勤の打刻
  - 勤務時間・残業時間の集計
  - 休暇・欠勤の申請と承認
  - 管理者向けの勤怠レポート／CSV エクスポート
  - ユーザー認証・権限管理（一般ユーザー／管理者）

> TODO: 実際のスコープが決まったら、この概要を更新してください。

---

## 2. 技術スタック

> TODO: 採用した技術に置き換えてください。以下は記入例です。

| 区分 | 技術 | 備考 |
|------|------|------|
| 言語 | （例: TypeScript / Python / Java） | |
| バックエンド | （例: Node.js + Express / FastAPI / Spring Boot） | |
| フロントエンド | （例: React / Vue / Next.js） | |
| データベース | （例: PostgreSQL / MySQL / SQLite） | |
| 認証 | （例: JWT / OAuth2 / セッション） | |
| テスト | （例: Jest / pytest / JUnit） | |
| Lint / Format | （例: ESLint + Prettier / Ruff / Black） | |
| CI/CD | （例: GitHub Actions） | |

---

## 3. ディレクトリ構成

> TODO: 実際の構成に合わせて更新してください。以下は一般的な例です。

```
attendance-system/
├── src/             # アプリケーションのソースコード
│   ├── api/         # API エンドポイント／ルーティング
│   ├── models/      # データモデル・スキーマ
│   ├── services/    # ビジネスロジック（勤怠集計など）
│   ├── repositories/# データアクセス層
│   └── utils/       # 共通ユーティリティ
├── tests/           # テストコード
├── migrations/      # DB マイグレーション
├── docs/            # ドキュメント
├── scripts/         # 補助スクリプト
└── CLAUDE.md        # 本ファイル
```

---

## 4. 開発ワークフロー

### 4.1 セットアップ

```bash
# TODO: 実際の手順に置き換える（例）
# 依存関係のインストール
# npm install   /   pip install -r requirements.txt   など

# 環境変数の設定
# cp .env.example .env

# データベースの初期化／マイグレーション
# npm run migrate   /   alembic upgrade head   など
```

### 4.2 よく使うコマンド

> TODO: package.json / Makefile などに合わせて更新してください。

| 目的 | コマンド（例） |
|------|----------------|
| 開発サーバ起動 | `npm run dev` |
| ビルド | `npm run build` |
| テスト実行 | `npm test` |
| 単一テスト実行 | `npm test -- <ファイル名>` |
| Lint | `npm run lint` |
| フォーマット | `npm run format` |
| 型チェック | `npm run typecheck` |

### 4.3 ブランチ運用

- `main`（または `master`）: 本番相当の安定ブランチ。直接コミットしない。
- 機能開発はフィーチャーブランチで行う（例: `feature/打刻機能`、`fix/集計バグ`）。
- プルリクエスト経由でマージし、レビューを必須とする。

> **AI アシスタントへ:** ユーザーから明示的な指示がない限り、勝手にプルリクエストを作成しないこと。コミット・プッシュは指示があった場合のみ行うこと。

---

## 5. コーディング規約

> TODO: チームの規約に合わせて具体化してください。

- 既存コードのスタイル（命名・コメント量・ファイル構成）に合わせる。
- 関数・変数名は意味が明確になるように付ける。
- ビジネスロジック（勤怠計算など）は `services/` に集約し、API 層・DB 層から分離する。
- 日時の扱いはタイムゾーンに注意する（保存は UTC、表示はローカル等の方針を明記すること）。
- マジックナンバー（勤務時間の閾値など）は定数として一元管理する。
- 例外・エラーは握りつぶさず、適切にログ出力・ハンドリングする。

---

## 6. テスト方針

- 新機能・バグ修正には対応するテストを追加する。
- 勤怠の集計ロジック（残業・深夜・休日判定など）は境界値テストを重視する。
- プッシュ前にテストと Lint がパスすることを確認する。

---

## 7. AI アシスタント向けの重要な注意事項

- このファイルの内容が古い場合は、作業の前に最新のコード構成を確認すること。
- 不明点や設計上の判断が必要な場合は、推測で進めず確認すること。
- 機密情報（パスワード、API キー、個人情報）をコミットしないこと。勤怠データには個人情報が含まれるため取り扱いに注意する。
- コミットメッセージは簡潔かつ説明的に記述すること。
- 大きなリファクタリングや破壊的変更は、事前に合意を得ること。

---

> このテンプレートはプロジェクトの成長に合わせて継続的に更新してください。
